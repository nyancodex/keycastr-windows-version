{
  "name": "keycastr-windows",
  "private": true,
  "version": "0.1.0",
  "description": "KeyCastr — Windows keystroke visualizer (Tauri port)",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
